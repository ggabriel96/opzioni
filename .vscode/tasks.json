{
    "tasks": [
        {
            "type": "shell",
            "label": "Build active file",
            "command": "ninja -C ${workspaceFolder}/build",
        }
    ],
    "version": "2.0.0"
}
