{
	"name": "opzioni",
	"build": {
		"dockerfile": "Dockerfile"
	},
	// "runArgs": [ "--cap-add=SYS_PTRACE", "--security-opt", "seccomp=unconfined"],
	"containerUser": "conan",
	"mounts": ["type=bind,source=${localEnv:HOME}/.ssh/,target=/home/conan/.ssh/"],

	"settings": { 
		"terminal.integrated.shell.linux": "/bin/bash"
	},

	"extensions": [
		"ms-vscode.cpptools",
		"asabil.meson",
		"eamodio.gitlens"
	]
}